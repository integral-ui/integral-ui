{
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --scope",
    "test": "lerna run test",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap",
    "commit": "git-cz",
    "update:stencil": "lerna add @stencil/core@latest --exact -D"
  },
  "name": "integral-ui",
  "devDependencies": {
    "commitizen": "^4.0.3",
    "cz-conventional-changelog": "^3.1.0",
    "lerna": "3.20.2"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
