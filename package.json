{
  "private": true,
  "workspaces": [
    "packages/**/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "update:stencil": "lerna run update"
  },
  "name": "integralui",
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
